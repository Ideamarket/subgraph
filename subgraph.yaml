specVersion: 0.0.2
schema:
  file: ./schema.graphql
dataSources:
  # ============================== IdeaToken ==============================
  - kind: ethereum/contract
    name: IdeaToken
    network: mainnet # Todo 
    source:
      abi: IdeaToken
    mapping:
      kind: ethereum/events
      apiVersion: 0.0.4
      language: wasm/assemblyscript
      entities:
        - Transfer
        - Approval
        - OwnershipChanged
      abis:
        - name: IdeaToken
          file: ./build/abis/IdeaToken.json
      eventHandlers:
        - event: Transfer(indexed address,indexed address,uint256)
          handler: handleTransfer
        - event: Approval(indexed address,indexed address,uint256)
          handler: handleApproval
        - event: OwnershipChanged(address,address)
          handler: handleOwnershipChanged
      file: ./src/IdeaToken.ts
  # ============================== IdeaTokenFactory ==============================
  - kind: ethereum/contract
    name: IdeaTokenFactory
    network: mainnet # Todo 
    source:
      address: "0x5a0b54d5dc17e0aadc383d2db43b0a0d3e029c4c" # Todo 
      abi: IdeaTokenFactory
    mapping:
      kind: ethereum/events
      apiVersion: 0.0.4
      language: wasm/assemblyscript
      entities:
        - NewMarket
        - NewToken
        - OwnershipChanged
      abis:
        - name: IdeaTokenFactory
          file: ./build/abis/IdeaTokenFactory.json
      eventHandlers:
        - event: NewMarket(uint256,string)
          handler: handleNewMarket
        - event: NewToken(uint256,uint256,string,address)
          handler: handleNewToken
        - event: OwnershipChanged(address,address)
          handler: handleOwnershipChanged
      file: ./src/IdeaTokenFactory.ts
